node_modules/
client/dist/

# verify harness output (screenshots, reports) â€” regenerated on every run
tools/verify/out/*
!tools/verify/out/.gitkeep
test-results/
playwright-report/

# env
.env
.env.*
!.env.example

# raw asset sources must never be committed (see CLAUDE.md Â§8)
*.blend
*.blend[0-9]*
*.fbx
*.obj
*.max
*.psd

.DS_Store
Thumbs.db

# raw asset pack sources — never committed (CLAUDE.md §8)
assets-src/
